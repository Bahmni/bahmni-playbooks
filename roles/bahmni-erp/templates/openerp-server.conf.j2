[options]
; This is the password that allows database operations:
; admin_passwd = admin
db_host = {{ bahmni_erp_db not in [False, 'localhost', '127.0.0.1'] and hostvars.get(bahmni_erp_db, {}).get('ansible_ssh_host') or bahmni_erp_db }}
db_port = False
db_user = openerp
db_password = False
